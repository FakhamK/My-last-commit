name: TEST pipeline

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    
    - name: docker build csharp
      run: |
        docker build . -t lastcommit:latest
